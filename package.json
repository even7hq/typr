{
  "name": "typr",
  "version": "1.0.0",
  "private": true,
  "description": "Interaction protocol built for humans and terminals. Monorepo workspace.",
  "workspaces": [
    "packages/js",
    "packages/docs"
  ],
  "scripts": {
    "build": "yarn workspace @typr/js build",
    "build:all": "yarn workspace @typr/js build && yarn workspace @typr/docs build",
    "docs:dev": "yarn workspace @typr/docs dev",
    "docs:build": "yarn workspace @typr/docs build",
    "docs:preview": "yarn workspace @typr/docs preview"
  },
  "license": "UNLICENSED"
}
