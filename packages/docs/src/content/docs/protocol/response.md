---
title: Response
description: How hosts answer REQUEST envelopes over NDJSON.
---

The response must repeat the same `correlationId` as the matching `REQUEST`. The prompt result is in `value` (type depends on the prompt).
